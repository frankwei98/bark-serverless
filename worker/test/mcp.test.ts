import { describe, it } from "vitest";

describe("mcp compatibility", () => {
  it.todo("supports /mcp with device_key supplied in tool arguments");
  it.todo("supports /mcp/:device_key with path-injected device_key");
});
